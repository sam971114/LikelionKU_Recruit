spring:
  datasource:
    url: jdbc:mysql://118.67.135.185:3306/likelion_recruit_db?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: $321RecruiT!#%&(
    driver-class-name: com.mysql.cj.jdbc.Driver
    read-only: false
  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false

  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQL5Dialect
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
        default_batch_fetch_size: 100

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.orm.jdbc.bind: trace
#  org.hibernate.type: trace

